# PR 作成先ポリシー

PR は常に `masa6161/arc-cli` (origin) に対して作成すること。

- Rich Haase 版 upstream に PR を作成してはならない
- `gh pr create` 実行時は `--repo masa6161/arc-cli` を明示指定する
- ユーザーから明示的にフォーク元への PR を指示された場合のみ、upstream を対象にしてよい
